# Runtime
NODE_ENV=development
PORT=3333

# Database (Docker compose defaults)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public
DATABASE_URL="postgresql://user:docker@localhost:5432/nest_clean?schema=public"

# JWT (RSA keys encoded in base64)
# To generate base64 from existing PEM files (macOS):
#   base64 -i private.pem | tr -d '\n' | pbcopy  # copies to clipboard
#   base64 -i public.pem  | tr -d '\n' | pbcopy
# Then paste below without quotes.
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
